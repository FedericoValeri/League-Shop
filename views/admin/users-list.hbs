<div class="row">
    {{#each users}}
    <div class="row">
        {{#each this}}
        <div class="col-sm-6 col-md-4" id="colonnaCarta">
            <div class="card" style="width: 18rem;">
                <div class="card-body">
                    <h5 class="card-title">Username: {{username}}</h5>
                    <h6 class="card-text">Email: {{email}}</h6>
                    <p class="card-text">id: {{_id}}</p>
                    <div class="row">
                        <div class="col-md-12">
                            <a href="/user/{{email}}" class="btn btn-info">
                                <span>Info Utente</span>
                            </a>
                            <form action="/admin/{{_id}}" method="post">
                                <div class="form-group">
                                    <button type="submit" class="btn btn-danger">Delete Account</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
    {{/each}}
</div>